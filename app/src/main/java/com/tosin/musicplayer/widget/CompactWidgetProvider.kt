package com.tosin.musicplayer.widget

/**
 * Thin subclass so the Android manifest can register this widget as a
 * separate entry in the widget picker. All behaviour is inherited from
 * [PlayerWidgetProvider].
 */
class CompactWidgetProvider : PlayerWidgetProvider()
